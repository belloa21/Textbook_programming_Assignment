#Alex Bello
#11/20/2019
#Defines the Book class and its functions


from Textbook_programming_person_class import Person


class Textbook:
    def __init__(self, author, edition, isbn_number, publisher, year_published, quantity_in_inventory, price ):
        self.author = Person
        self.edition = edition
        self.isbn_number = isbn_number
        self.publisher = publisher
        self.year_published = year_published
        self.quantity_in_inventory = quantity_in_inventory
        self.price = price

    def add_to_inventory(self, quantity_in_inventory):
        quantity_in_inventory +=
